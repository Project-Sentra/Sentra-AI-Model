# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0

# ML and Computer Vision
# NOTE: PyTorch CPU-only is installed separately in Dockerfile via --index-url
# to avoid pulling the 2GB CUDA build. See Dockerfile Stage 1.
ultralytics>=8.0.0
easyocr>=1.7.0
# opencv-python-headless: no GUI/Qt dependencies — required for Docker (no display)
opencv-python-headless>=4.8.0
numpy>=1.24.0

# ONNX Runtime — lightweight inference engine for quantized .onnx models
onnxruntime>=1.16.0

# HTTP client
httpx>=0.25.0
aiohttp>=3.9.0

# Image processing
Pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
